export interface Parametros{
    fechaini ?: string;
    fechafin ?: string;
    usuario ?: string;
    contrasena ?: string;
    empresa ?: string;
    capana ?: string;
  }
  //? cuando llame la interface se opcional