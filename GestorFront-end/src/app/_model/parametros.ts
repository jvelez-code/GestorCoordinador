export interface Parametros{
      fechaini ?: string;
      fechafin ?: string;
      usuario ?: string;
      contrasena ?: string;
      empresa ?: string;
      campana ?: number;
  }
  //? cuando llame la interface se opcional